{
  "name": "NUB Userbot",
  "description": "A powerful, highly modular, and clean Pyrogram based Telegram Userbot.",
  "logo": "https://raw.githubusercontent.com/nub-coders/nub-userbot/main/userbot.jpg",
  "keywords": [
    "telegram",
    "userbot",
    "pyrogram",
    "python",
    "nub"
  ],
  "repository": "https://github.com/nub-coders/nub-userbot",
  "env": {
    "API_ID": {
      "description": "Your Telegram API ID from my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API Hash from my.telegram.org",
      "required": true
    },
    "SESSION_STR": {
      "description": "Your Pyrogram String Session",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Optional: A Bot Token from @BotFather (used for inline bot features)",
      "required": false
    },
    "GEMINI_API_KEY": {
      "description": "Optional: Google Gemini API Key from https://aistudio.google.com/app/apikey (needed for AI features)",
      "required": false
    },
    "GROUP": {
      "description": "Your support group username (without @)",
      "value": "nub_coder_s",
      "required": false
    },
    "CHANNEL": {
      "description": "Your updates channel username (without @)",
      "value": "nub_coders",
      "required": false
    },
    "USERBOT_USERNAME": {
      "description": "The username of your userbot account (without @) to use in menus",
      "required": false
    },
    "YT_DLP_API_KEY": {
      "description": "Optional: API Key for YT_DLP to download YouTube videos",
      "required": false
    },
    "YT_DLP_BASE_URL": {
      "description": "Optional: Base URL for YT_DLP API (defaults to http://api.nubcoders.com)",
      "value": "http://api.nubcoders.com",
      "required": false
    },
    "MONGO_URI": {
      "description": "Optional: MongoDB connection URI for persistent storage (falls back to in-memory storage if unset)",
      "required": false
    },
    "DB_NAME": {
      "description": "Optional: MongoDB database name",
      "value": "userbot",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    },
    {
      "url": "https://github.com/heroku/heroku-buildpack-activestorage-preview"
    }
  ],
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "eco"
    }
  }
}
